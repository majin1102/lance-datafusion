# Example Lance namespace configuration (directory type)
# Demonstrates CLI --config loading

# Root namespace: dynamic CatalogProviderList
lance.root.type: directory
lance.root.path: /path/to/lance_root

# Additional catalog: crm
lance.catalog.crm.type: directory
lance.catalog.crm.path: /path/to/crm_namespace
